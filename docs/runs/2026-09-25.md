# Pickle runs of 2026-09-25 (toward 1.0.1)

Raw output on disk in `Tests/Pickle/Evidence/2026-09-25-*` (ignored by git; JPEG captures, gzipped logs).

| Run | Language | Result | What it showed |
|---|---|---|---|
| `b182` (pass 2b v5, cooking film, request of 2026-09-24 21:12, tree `507902c`) | French | **no report**: the watchdog ended the game at 120 s, exit 3 | The launcher's text said the start never finished; the kept `Player.log` says otherwise: the scenario ran, and the last step was "the cook works for up to 45 seconds or until a meal is cooked". What ended the run is the scenario watchdog, `-pickle-scenario-timeout` (120 s by default, one scenario), which the launcher does not pass; `@timeout:N` is a different limit (a per-step default), so moving it from the Feature line to the Scenario line in `7b96007` changed nothing about the kill. The game ran at about 13 ticks a second on the loaded machine (597 ticks in 45 s) because the feature carries `@watch` (real-time waits), so the cook, with a bill, 50 fuel, 12 squirrel meat and 12 milk, had not finished. Explained by the PickleTools session, who read Pickle's sources (`PickleTools/Authoring/README.md`, "Waiting: fast mode, @watch, and three different timeouts"); their copy of the sources is not established as v4.9.1 and nothing was played. Remedy now used: the request passes `-Extra '-pickle-scenario-timeout=600'`. |
| `d407` (pass 2b v6, cooking film, tree `7b96007`) | French | **no report**: killed from outside, exit 143 | The TicketDispatcher's own test of `stop-game-wsl.sh` ran `pkill -f xvfb-run` while this game was running. Nothing proved, for or against; not analysed. Redeposited as `1c24` (pass 2b v7, tree `ad6bd9b`), the 31st and last request of the queue, then cancelled before it ran and replaced by `c4c3` (pass 2b v8, tree `3d7423a`, `-Extra '-pickle-scenario-timeout=600'`), again last. |
| `63b1` (1.0.1, the two lavish meals of feature 07, filter `07-review-shots.feature::lavish meal`, `sans-facultatifs`, ScreenshotMode, 12:51) | French | 2/2, `exitReason: passed`, exit 0, one attempt | The 1.0.1 fix seen in a game: the side dishes read in lower case after a French joint, "Ragoût mulligan et kakigōri" and "Barre énergétique aux pommes de terre avec yaourt" (the new step `the side dishes ... do not start with a capital after a French joint` passed on both). The two info cards were opened: French from title to properties, side dish and description agree. **Limits:** no negative control on the unfixed build, and the dishes are drawn at random, so this shows the fix working on two meals, not on every dish. The label says `b292667` but the tree staged was later (`Mod/` then differed by the mod name and the Preview only). |
| `6072` (pass 2b v9, cooking film, feature 09, `cuisson-film` map, 600 s scenario watchdog, tree `5b99cfa`, 16:44) | French | 1/1, `exitReason: passed`, exit 0, one attempt, 200 s | The cooking pass is green: a colonist cooks at a fuelled stove and the meal is named in the language of the pass. Film (`film.webm`) and the colony-view capture kept under `Tests/Pickle/Evidence/2026-09-25-pass2b-cooking-v9/`; the two `did not load any content` errors in `Player.log` are the test mods, which carry no defs. The owner judges the French on the film. |
| `e893` (non-regression pass 1, English, `sans-facultatifs`, features 01, 02, 04, 05, 06, 19, after the 1.0.1 publication; `Mod/` and `Tests/` identical to `256d704`, checked by `git diff`) | English | 21/21, `exitReason: passed`, exit 0, one attempt | The non-regression replay of pass 1 is green after 1.0.1. The archive `0925-1942` was deleted after its report was kept in `Tests/Pickle/Evidence/2026-09-25-nonreg-pass1-english/`. Pass 2 (French) requested next as `b7be`. |
| `b7be` (non-regression pass 2, French, `sans-facultatifs`, seven launches, after the 1.0.1 publication; `Mod/` and `Tests/` identical to `256d704`) | French | 27/27, seven launches all `exitReason: passed`, exit 0, one attempt: 03 5/5, 01 5/5, 04 4/4, 05 2/2, 06 3/3, 19 3/3, 07 5/5 | The non-regression replay of pass 2 is green after 1.0.1, so passes 1 and 2 are green in both languages on the published tree. Reports kept in `Tests/Pickle/Evidence/2026-09-25-nonreg-pass2-french/seq1..7` (105 MB); the archive of launch 7 was deleted, the others had been purged by the dispatcher. Pass 5 requested as `0d0b`. |
