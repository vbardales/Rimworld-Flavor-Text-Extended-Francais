# Pickle runs of 2026-09-25 (toward 1.0.1)

Raw output on disk in `Tests/Pickle/Evidence/2026-09-25-*` (ignored by git; JPEG captures, gzipped logs).

| Run | Language | Result | What it showed |
|---|---|---|---|
| `b182` (pass 2b v5, cooking film, request of 2026-09-24 21:12, tree `507902c`) | French | **no report**: the watchdog ended the game at 120 s, exit 3 | The launcher's text said the start never finished; the kept `Player.log` says otherwise: the scenario ran, and the last step was "the cook works for up to 45 seconds or until a meal is cooked". The `@timeout:300` tag sat on the Feature line, where Pickle does not honour it (120 s by default), and the game ran at about 13 ticks a second on the loaded machine (597 ticks in 45 s), so the cook, with a bill, 50 fuel, 12 squirrel meat and 12 milk, had not finished. Moved to the Scenario line with `@timeout:420` and eight slices (`7b96007`). |
| `d407` (pass 2b v6, cooking film, tree `7b96007`) | French | **no report**: killed from outside, exit 143 | The TicketDispatcher's own test of `stop-game-wsl.sh` ran `pkill -f xvfb-run` while this game was running. Nothing proved, for or against; not analysed. Redeposited as `1c24` (pass 2b v7, tree `ad6bd9b`), which is the 31st and last request of the queue. |
| `63b1` (1.0.1, the two lavish meals of feature 07, filter `07-review-shots.feature::lavish meal`, `sans-facultatifs`, ScreenshotMode, 12:51) | French | 2/2, `exitReason: passed`, exit 0, one attempt | The 1.0.1 fix seen in a game: the side dishes read in lower case after a French joint, "Ragoût mulligan et kakigōri" and "Barre énergétique aux pommes de terre avec yaourt" (the new step `the side dishes ... do not start with a capital after a French joint` passed on both). The two info cards were opened: French from title to properties, side dish and description agree. **Limits:** no negative control on the unfixed build, and the dishes are drawn at random, so this shows the fix working on two meals, not on every dish. The label says `b292667` but the tree staged was later (`Mod/` then differed by the mod name and the Preview only). |
