# Pickle runs of 2026-09-21

Text summary. The raw output (Player.log, junit.xml, messages.ndjson, captures) is kept on disk in
`Tests/Pickle/Evidence/2026-09-21-*` and ignored by git. Suite as it stood that day, run in the WSL
through `Run-PickleWsl.ps1`. These runs predate `tested`; they show what happened, not a certification.

| Run | Language | Result | What it showed |
|---|---|---|---|
| `english` | English, before the language fix | 24 scenarios: 18 passed, 0 failed, 6 skipped | The six skipped are feature 03, meant for the French pass. The two settings captures (from Mod options and from the shortcut) were opened and were correct. |
| `french-03` | French, before the fix | 6 scenarios: 4 passed, 2 failed | Failed: "the wrapper let the French operations through" and "the category override holds the French forms". The first exposed the language-guard defect (`French (Français)` never matched `French`); the second was a limit of Pickle's stock steps. |
| `french-03b` | French, before the fix | 5 scenarios: 4 passed, 1 failed | Failed: "the wrapper let the French category overrides through", same defect. |
| `french-ok` (5 launches under one lock) | French, after the fix (`164104b`) | 19 scenarios, all passed | seq1 feature 03 5/5; seq2 feature 01 5/5; seq3 feature 04 4/4 (shortcut, Options entry, cap clamp, reveal/hide); seq4 feature 05 2/2; seq5 feature 06 3/3 (cow, squirrel, husky). The meal captures were validated by the owner (squirrel, beef, first lavish meal); the husky one did not test the aspirated h because the dish drawn did not name the husky, which is what feature 19 forces. |

Later runs (`tested` passes) are summarised in their own file under `docs/runs/`.

All the raw 2026-09-21 folders were deleted on 2026-09-23 under the root AGENTS.md evidence rule, superseded by the
replays of `2026-09-23.md`; the results are the table above.
