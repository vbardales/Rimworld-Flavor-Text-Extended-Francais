# Pickle runs of 2026-09-23 (toward `tested`)

Raw output kept on disk in `Tests/Pickle/Evidence/2026-09-23-*` (ignored by git; JPEG captures, gzipped logs).

| Run | Language | Result | What it showed |
|---|---|---|---|
| `pass1-english` (`sans-facultatifs`, ScreenshotMode, launched at 20:26, suite at `846c156`) | English | `exitReason: passed`; 50 scenarios discovered: 21 passed, 0 failed, 29 skipped; all 14 staged mods loaded | Features 01, 02, 04, 05, 06 and the new 19 played in full. The 29 skipped are the French scenarios of 03 and the `@wip` features still to be un-wipped in their own passes. The three forced dishes of 19 held (no "no longer matches" warning): "Baked Squirrel Meat", "Baked Husky Meat", "Baked Beef"; the French shape is only asserted in the French pass. Random names: "Barbecued Beef", "Squirrel Stroganoff", "Husky Chowder". The five clean captures (settings from Mod options and from the shortcut, three info cards) were opened: no HUD, no Pickle panel, English text. Log: only the two expected "did not load any content" lines of the companion mods. |

Pending in the passes to come: French shape of feature 19, `@wip` removal, F13 fixture.

| `pass2-french` (`sans-facultatifs`, ScreenshotMode, French, seven launches under one lock 22:54-23:08, suite `846c156`) | French | Every launch `exitReason: passed`: 03 5/5, 01 5/5, 04 4/4, 05 2/2, 06 3/3, 19 3/3, 07 5/5 (27 scenarios, 0 failed, 0 skipped); 14 mods loaded where a save is loaded; 07's game exited with 137 after a complete report (5 passed, 0 failed), the launcher kept the report | The forced dishes read in French: "Viande d'écureuil au four", "Viande de husky au four" (no elision before the aspirated h), "Viande de bœuf au four". Random names: "Salade de lait", "Salade d'écureuil", "Stroganoff de husky", "Dolma, façon Œufs de poule tournés à jaune coulant", "Funeral potatoes avec Huîtres des Rocheuses" (kept in English by decision). Captures opened: the settings page from Mod options is fully French (Plafond d'ingrédients supplémentaires, Recherche rapide, Nommer les piles de repas, Correspondance souple des recettes, Détection dynamique des repas, Fermer); the lavish info card is French from title to properties, with the inspect pane of the selected meal still drawn at the bottom left. |

Evidence lost: the launcher's archive folders of launches 1 to 6 were purged by the machine's disk clean-up before I copied them (the shared `pickle-reports` had already moved on). What is kept: the launcher output with every launch's counts and `exitReason` (`launcher-output.txt`), launch 7's full report, all captures, and the names read from the logs of 5 and 6 before the purge. Lesson for the next passes: copy each launch's report as soon as the lock is released, not after the whole sequence.
