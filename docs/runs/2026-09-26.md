# Pickle runs of 2026-09-26 (after 1.0.1)

Raw output on disk in `Tests/Pickle/Evidence/2026-09-26-*` (ignored by git).

| Run | Language | Result | What it showed |
|---|---|---|---|
| `3a5a` (pass 2b v10, cooking film with the info card of the cooked meal, tree `245898d`, `-Extra '-pickle-scenario-timeout 600'`) | French | **no verdict**: the game's scenario watchdog ended it at 120 s, exit 3 (launcher: "Terminated") | The 15 staged mods loaded. Three 45 s slices of the cook passed with no meal yet (about 900 ticks each), then `pickle: watchdog tripped after 120s`. The flag reached Pickle without its `=`: the launcher's own regexp accepts a space, but the game ran on its 120 s default. Nothing proved for or against the mod. Redeposited with `-pickle-scenario-timeout=600` as `1ed7`. |

Disk cleanup of 2026-09-26 (the disk was at 1.3 GB free, the Pickle queue stopped below 2 GB): deleted the superseded or no-verdict report folders (2026-09-23 pass 1 and 2, pass 2b v5, v6 and v10), compacted the kept ones (PNG captures re-encoded as JPEG at quality 85, `report.html` dropped; summaries, junit, logs and film kept) and removed the regenerable `.build`. `Tests/Pickle/Evidence/` went from 187 MB to 12 MB. The passes 3, 4, 6 to 10 and 20 reports stay: no later run repeats them.
