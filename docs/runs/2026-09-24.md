# Pickle runs of 2026-09-24 (toward `tested`)

Raw output on disk in `Tests/Pickle/Evidence/2026-09-24-*` (ignored by git; JPEG captures, gzipped logs).

| Run | Language | Result | What it showed |
|---|---|---|---|
| `pass2b` (cooking, film) | French | **did not finish**: the game was killed by Pickle's watchdog at 120 s, in `I wait for bill "CookMealSimple" to finish`; no report, launcher exit 3 | The stock bill wait allows 120 real seconds and the cook had not finished. What the cook, the stove and the stock were doing is unknown: the log says nothing. Feature 09 now waits in six slices of 45 s with a diagnostic (`6dbac03`); pass 2b is queued again. |
| `pass3-french-providers` (`avec-facultatifs`, ScreenshotMode, 26 mods loaded, suite `846c156`) | French | 06 3/3 and 01 5/5, both `exitReason: passed` (the 06 game exited with 137 after a complete report, the launcher kept it) | Names: "Fricassée de bœuf", "Boisson santé d'écureuil", "Ragoût de lait". The squirrel info card was opened: French from title to properties. The `[ERROR]` lines of the log are `Bow_... is made from stuff but has no stuff set` (a provider mod's things in the save), none names this mod. **Limit:** cow, squirrel and milk never touch a provider table, so this run shows the providers load beside the mod and nothing more; feature 20 (`799e7e2`) reads the provider tables themselves and is queued for passes 3 and 4. |
| `pass4-french-shenzhou` (`avec-shenzhou`, 15 mods staged, 00:54-01:03, suite `846c156`) | French | 06 3/3 and 01 5/5, both `exitReason: passed` (both games exited with 137 after a complete report) | Names: "Paprikash à la viande de bœuf", "Pouding d'écureuil", "Chaussons au lait". The launcher flags `dajian.chiteaditional.expanded` as dropped by the game; the log shows why and that it is a false alarm: the game lists it as `(incompatible version)` (it declares 1.5 at most) but its content loaded and ran, and its own errors are in the log (`Plant_LianOu` textures, `HSJZChineseCulture.Patch.PatchMain`, XML errors of `Chi_Core`), none of them naming this mod. As with pass 3, these meals never use a Shenzhou ingredient: feature 20 reads the four Shenzhou entries and is queued. |
| `pass5-french-invented-foods` (`faux-ingredients`, 15 mods loaded, 01:04-01:08, suite `846c156`) | French | 08 4/4, `exitReason: passed` (the game exited with 137 after a complete report) | Names for the three invented foods: "Kakigōri" (bare word yuzu with milk), "Yukhoe" (huile de noix with squirrel), "Rôti de bœuf et de haricots rouges" (plural compound). The fallback forms produced readable French names and the run logged no error of this mod. The load logged `Config error in FTFR_...: no foodType` for the three invented things: a defect of the fixture, fixed in the fixture (each now carries a foodType), so pass 5 is replayed in the final round. |
| `pass6-restart-pair` (English, 01:09-01:12, two launches under one lock, suite `846c156`) | English | Launch 1 (10, values written): 1/1; launch 2 (11, values read after a real restart, then put back): 2/2; both `exitReason: passed` | The two settings changed in launch 1 (ingredient cap and quick search) survived the restart and were read back in launch 2, whose last scenario put the defaults back and wrote them, leaving the profile as it was found. The only `[ERROR]` line of either log is the companion's harmless "did not load any content". |
| `pass7-rimmsqol` (English, `avec-rimmsqol`, 16 mods loaded, 01:12-01:24, four launches under one lock, suite `846c156`) | English | 12: 3/3; 13: 1/1; 14: 1/1; 15: 1/1, every launch `exitReason: passed` (launches 1 and 3 exited with 137 after a complete report) | RIMMSQOL's own main-button list offers "Flavor Text settings" (hidden, not drawn on the bar); revealed, the shortcut is drawn and opens the same page as Mod options; the choice survived a real restart; hiding it survived a second restart; the last scenario forgot RIMMSQOL's choice and put its configuration back. Captures opened: RIMMSQOL's list with the entry, and the settings page opened by the revealed shortcut (clean, five settings, no HUD). |
| `pass8-without-biotech` (English, `sans-biotech`, 12 mods staged, 01:24, no save loaded, suite `846c156`) | English | 16: 2/2, `exitReason: passed` | The launcher staged the pass without `ludeon.rimworld.biotech` ("sans le DLC"), so the `!packageId` removal of a DLC works in a real run; "Biotech is left out, and so is its dish" passed, and the game started and loaded the mod without an error. |
| `pass9-without-anomaly-odyssey` (English, `sans-anomaly-odyssey`, 11 mods staged, 01:26, no save loaded, suite `846c156`) | English | 17: 2/2, `exitReason: passed` | The launcher removed both `ludeon.rimworld.anomaly` and `ludeon.rimworld.odyssey` ("sans le DLC" twice), leaving Core, Royalty, Ideology and Biotech; "both DLC are left out and the mod is loaded" and "the game started and loaded the mod without an error" passed. Only `[ERROR]` line: the companion's harmless "did not load any content". |
| `pass2b-v2` (cooking, film, French, 08:17, sliced wait) | French | **failed**, `exitReason: failed`, 0/1, in 41.6 s: "the meal was not renamed: it reads 'ration de survie x10', which is not '<dish> (ration de survie)'" | No longer a watchdog kill: the scenario ran to its assertion. But the step that waits for a cooked meal returned after 3.7 s of film because the colony of the fixture already holds survival rations (a `Meal...` def carrying Flavor Text's comp), which the step took for the cook's dish; the assertion then rightly refused a ration. Not a defect of the mod. Fixed in the step (`627c81c`): the meals present before the stove was lit are recorded, and only a `MealSimple`, `MealFine` or `MealLavish` that appeared after counts. Pass 2b queued a third time. |
| `pass3b-provider-tables` (`avec-facultatifs`, French, 26 mods loaded, 08:21, feature 20, suite `627c81c`) | French | 20: 1 passed, 1 skipped, `exitReason: passed`; the skipped one is the Shenzhou scenario, which requires a mod this pass does not stage (it plays in pass 4) | The provider tables read as this mod wrote them: 12 tables checked (Optimization: Meats, VGP Vegetable Garden and Garden Gourmet, Vanilla Plants Expanded More Plants, Vanilla Cooking Expanded, RH2 Faction V.O.I.D., New Harvest and its Extended entry, RC2 Core, Kit's Brazilian Crops, Medieval Overhaul, TP Sea Plants), none skipped, 30 ingredients present in the game, each with four forms in Flavor Text's own table. The tables of the three providers left out of the staging on the owner's word still exist as data defs, so they were checked too, but their ingredients are absent from the game: the 30 present ones come from the providers actually staged. |
| `pass4b-provider-tables` (`avec-shenzhou`, French, 08:27, feature 20, suite `627c81c`) | French | **failed**, `exitReason: failed`: "only 1 ingredients named by the loaded tables exist in the game, expected at least 4" | A defect of the step, not of the mod: it read one table per patch operation, and `Inflections_ExtendedProviders_FR.xml` adds many tables in ONE operation (one element each), so it checked the same 12 tables as pass 3 and none of Shenzhou's (the only ingredient found was Meat_Cow, from the Optimization: Meats table). The step now reads every table of an operation (`668014e`); pass 4b queued again. |
| `fixture-maker` (feature 21, English, `sans-facultatifs`, 13 mods, 09:43) | English | 1/1, `exitReason: passed` (a tool, not a pass) | Three meals named then saved: "Milk Salad" and "Squirrel Salad" (fine), "Rice Power Bar and Chicken Eggs Over-Medium" (lavish). The save `legacy-meals-before-ftfr.rws` (3.1 MB) holds three `finalFlavorDefs` and lists `hekmo.flavortext` and this mod among its mods (loaded in English, where it changes nothing). Committed as `Tests/Pickle/Mod/Pickle/Fixtures/legacy-meals-before-ftfr.rws` (`f07f576`); feature 18 lost its last `@wip` and is queued as pass 10. |
| `pass2b-v3` (cooking, film, French, 09:46, suite `627c81c`) | French | **failed**, `exitReason: failed`, 0/1, 42.2 s: "Accessing map pawns off main thread - this is never allowed due to list pooling" | Pickle's runner reported an error raised by the game's own guard, and no `[FTFR tests]` line of the cooking steps was logged. The two things this feature's steps walk that touch pawns are `listerThings.AllThings` (which holds the pawns) in the before-cooking snapshot and `mapPawns` in the diagnostic after a slice. Both removed (meals are read def by def, the diagnostic no longer reads the cook). Not confirmed as the cause until the next run: if it fails again the log will be read for the step. Queued a fourth time. |
| `pass4b-v2-provider-tables` (`avec-shenzhou`, French, 11:03, feature 20, suite `668014e`) | French | 20: the Shenzhou scenario passed, the pass-3 scenario skipped (its provider is not staged here), `exitReason: passed` | 16 tables read as this mod wrote them: the 12 of pass 3 plus `FTFR_Extended_dajian_chiteaditional_expanded`, its `_oldmode` twin, `FTFR_Extended_daylight_rimlifeextrgmod` and `FTFR_Extended_daylight_rlevvcultivationplus` (the tables are all loaded as data; the Shenzhou ingredients exist in the game); 39 ingredients present, each with four forms in Flavor Text's own table. The launcher again flags Shenzhou as dropped (false alarm, see pass 4). |
